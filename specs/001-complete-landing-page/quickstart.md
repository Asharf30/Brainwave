# Quickstart

1. Run `npm run dev`
2. Open `http://localhost:5173`
3. Verify all 10+ sections load.
4. Scroll to verify GSAP animations trigger correctly.
5. Check responsiveness on mobile and tablet viewport sizes.
